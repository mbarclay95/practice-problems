
const geometry = 6;
const algebra = 7;
const physics = 8;
// const english = 4;

console.log(`Average score: ${getAverage(geometry, algebra, physics)}`);

function getAverage(x: number, y: number, z: number): number {
    // calculate the average
    return 0;
}

// function getAverageUsingArray(): number {
//
// }
